{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "dependencies": {
    "az": "github:VictoriaBoyarkina/Az.js",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "graph-modal": "github:VictoriaBoyarkina/graph-modal",
    "socket.io": "^4.7.5"
  },
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "serve -s ./frontend/dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci"
  },
  "devDependencies": {
    "webpack-cli": "^5.1.4"
  }
}
